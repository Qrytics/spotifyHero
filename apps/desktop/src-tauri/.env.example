# Copy to `.env` and set your Client ID from https://developer.spotify.com/dashboard
# Redirect URI in the dashboard must match exactly: http://127.0.0.1:8888/callback
# Development mode: add your Spotify account email under App → Settings → User Management.

SPOTIFY_CLIENT_ID=
